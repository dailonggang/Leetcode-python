def swapPairs(head):
    dummy = ListNode()
    dummy.next = head
    pre = dummy
    while pre.next and pre.next.next:
        cur = pre.next
        nxt = pre.next.next

        cur.next = nxt.next
        nxt.next = cur
        pre.next = nxt

        pre = cur
    return dummy.next
